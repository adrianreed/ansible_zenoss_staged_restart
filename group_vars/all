# System user with SSH access to the Zenoss hosts (that ansible will use to connect)
ssh_user:
# Zenoss user with permissions to manage serviced through systemctl and the serviced command
zenoss_user:
